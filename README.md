# vLLM hybrid Mamba / GDN field notes

English field report from a home-lab stack that actually ran
**Qwen3.8-27B (hybrid GDN) + vLLM + MTP + prefix cache + LMCache**.

The combination everyone wants is also the combination that silently
corrupts output.

**Read [FIELD_NOTES.md](FIELD_NOTES.md).** That file is the report.

This repo is documentation only. No patches, no binaries, no configs
with secrets.
